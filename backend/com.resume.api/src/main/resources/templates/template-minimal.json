{
  "code": "minimal",
  "name": "极简留白",
  "description": "大留白、少装饰，突出文字内容本身。",
  "schema": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "contact": {
        "type": "object",
        "properties": {
          "email": { "type": "string" },
          "phone": { "type": "string" }
        }
      },
      "highlights": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["name"]
  },
  "html": "<div class=\"resume-minimal\"><h1>{{name}}</h1><p>{{contact.email}}</p><p>{{contact.phone}}</p><ul>{{#each highlights}}<li>{{.}}</li>{{/each}}</ul></div>",
  "css": ".resume-minimal{letter-spacing:.05em;line-height:1.8}"
}
